export const SuccessMessage = () => {
  return (
    <div
      className={` transition-all w-full font-medium text-lg  flex items-center justify-center transform duration-500`}
    >
      Dear customer, payments are current disabled due to security reasons.
      Please click on the chat Icon at the bottom right corner to speak to our
      customer service agent 👇
    </div>
  );
};
