export const ErrorMessage = () => {
  return (
    <div
      className={` transition-all w-full font-medium text-lg  flex items-center justify-center transform duration-500`}
    >
      Payment Request Failed, Please Try again or click the chat Icon at the
      bottom right corner to speak to our customer service agent 👇
    </div>
  );
};
